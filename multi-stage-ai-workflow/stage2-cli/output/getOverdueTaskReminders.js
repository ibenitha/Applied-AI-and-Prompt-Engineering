/**
 * Overdue Task Reminders
 *
 * Surface a visible count of overdue tasks to the user as soon as they open TaskFlow, instead of requiring them to scroll through the full task list to notice missed deadlines.
 *
 * Acceptance criteria to satisfy:
 * 1. TODO: A task counts as overdue when its due date is earlier than the current date and its status is not 'done'.
 * 2. TODO: The app displays a badge showing the count of overdue tasks near the task list header.
 * 3. TODO: The badge is hidden, or shows 0, when there are no overdue tasks.
 * 4. TODO: The overdue count updates automatically whenever a task's status or due date changes.
 *
 * NOTE: generated as a stub by Stage 2 (CLI). Stage 3 (IDE AI) fills in the
 * implementation below.
 */
function getOverdueTaskReminders(tasks, now = new Date()) {
  // TODO: implement — see acceptance criteria above.
  throw new Error("Not implemented yet");
}

module.exports = { getOverdueTaskReminders };
